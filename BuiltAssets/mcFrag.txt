#version 330 core

uniform sampler2D depthTex;
uniform vec4 col;

in vec3 v2f_col;
in vec3 v2f_spos;

out vec4 fragCol;

void main () {
	//if (v2f_spos.z > (texture(depthTex, v2f_spos.xy * 0.5 + 0.5).z*2-1)) discard;
	fragCol = vec4(v2f_col, 1);
}